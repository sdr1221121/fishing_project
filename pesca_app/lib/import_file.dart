import 'package:flutter/material.dart';

class ImportFileExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Import File Example'),
      ),
      body: Center(
        child: Text('This is an example of an imported file widget.'),
      ),
    );
  }
}